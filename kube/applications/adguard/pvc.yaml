---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard
  namespace: applications
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
