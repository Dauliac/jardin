---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: download
  namespace: applications
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  volumeName: download
