---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: applications
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
  volumeName: media
